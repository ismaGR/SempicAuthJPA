docker-machine stop && docker-machine start && sleep 10 && docker-machine env && sleep 4  && eval $(docker-machine env)

docker run -d -v $( echo $PWD ):/home/SempicAuthJPA  -p8888:8080 -p9001:9001 deb_buster

[[ $(pgrep java) > 1 ]]&& kill $(pgrep java) && cd /home/SempicAuthJPA/ && rm -rf ./target && mvn package tomee:exec && sleep 2 && ./target/apache-tomee/bin/startup.sh && sleep 4 && tail -f  ./target/apache-tomee/logs/catalina.2020*.log